<Project>

	<PropertyGroup>
		<MSBuildAllProjects>$(MSBuildAllProjects);$(MSBuildThisFileFullPath)</MSBuildAllProjects>
	</PropertyGroup>

	<ImportGroup>
		<!--<Import Project="$(MSBuildThisFileDirectory)MSBuild.NET.Extras.Core.targets" />-->
		<Import Project="$(MSBuildThisFileDirectory)MSBuild.NET.Extras.Common.props" />
		<Import Project="$(MSBuildThisFileDirectory)NuGet.Workarounds.targets" Condition="'$(_SdkDotNetVersion)' &lt; '2.0'" />
	</ImportGroup>

</Project>