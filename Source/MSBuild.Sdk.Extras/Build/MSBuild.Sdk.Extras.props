<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003">

  <PropertyGroup>
    <MSBuildAllProjects>$(MSBuildAllProjects);$(MSBuildThisFileFullPath)</MSBuildAllProjects>
  </PropertyGroup>

  <Import Project="$(MSBuildThisFileDirectory)MSBuild.NET.Workarounds.props"/>
  <Import Project="$(MSBuildThisFileDirectory)MSBuild.NET.Extras.Sdk.props"/>

  <PropertyGroup>
    <MSBuildSdkExtrasTargets>$(MSBuildThisFileDirectory)MSBuild.NET.Platforms.targets</MSBuildSdkExtrasTargets>
  </PropertyGroup>

</Project>